import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'l2e-official-app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
